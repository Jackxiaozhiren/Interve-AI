# TECH_RESEARCH (Phase 1; refreshed 2026-09-13)

> Priority: official docs > standards bodies > original papers > high-quality tech sources > blogs.
> Refresh basis: HEAD `f37f574` + working tree (Phases 2-13 landed uncommitted); local `node_modules/next/dist/docs` read directly per AGENTS.md; Context7 `/vercel/ai` + `/vercel/next.js` (canary); live web search 2026-09-13.
> Previous version 2026-09-03 retained below as §A; §B is the 2026-09-13 delta. Decisions are binding unless a later phase records a reversal with basis.

## §A — 2026-09-03 baseline (unchanged, still valid)

| # | Topic | Source | Date / Version | Decision for Interve AI | Reason |
|---|---|---|---|---|---|
| 1 | Next.js `proxy.ts` vs `middleware.ts` | Context7 `/vercel/next.js` (canary build/index.ts, v16 upgrade guide, proxy test fixture) | 2026-09-03 / Next 16.2.x | Fix `src/proxy.ts`: use named `export function proxy`, keep nodejs runtime (edge NOT supported in proxy); do not keep both files | Current file uses `export default`, likely inert; build enforces mutual exclusivity; official codemod `middleware-to-proxy` |
| 2 | AI SDK retries/timeout/abort | Context7 `/vercel/ai` (settings `RequestOptions`: `maxRetries` default 2, `abortSignal`, `timeout.totalMs/stepMs/firstChunkMs`) | 2026-09-03 / AI SDK 6 | Set explicit `maxRetries`, `AbortSignal.timeout` + `timeout.totalMs`, forward `req.signal`; add server model allowlist; no client-controlled `model` | Current 0/16 explicit retries, 2/16 abort passthrough, fallback causes 4-6 calls on outage |
| 3 | Supabase RLS owner pattern | Context7 `/supabase/supabase` (RLS guide: `to authenticated using ((select auth.uid()) = user_id)`, one policy per op) | 2026-09-03 | Add `user_id uuid REFERENCES auth.users`, per-op `select/insert/update/delete` policies, never `FOR ALL USING(true)` | Current 7× `USING(true)` anon-readable; no `user_id` columns |
| 4 | OWASP LLM Top 10 2025 | owasp.org PDF + genai.owasp.org LLM01/LLM05 | 2025 v1.1 | Segregate untrusted resume/JD/transcript (delimiters + ignore-instruction), Zod inputs, encode LLM output, CSP, rate-limit, monitor | All 16 routes concatenate untrusted content; `dangerouslySetInnerHTML` without sanitize (LLM01/LLM05) |
| 5 | EU AI Act | EUR-Lex 2024/1689 + AI Act Service Desk Art.5/Annex III + regulatoryai.eu | In force; Art.5(1)(f) ban since 2025-02-02; high-risk hiring duties 2027-12-02 | Remove face/voice emotion→hire path; replace `hireVerdict/cultureFit` with practice readiness + disclaimer | Fake vision + `hireVerdict`/`culturalTraits` is prohibited-pattern (workplace emotion inference) and high-risk hiring |
| 6 | NYC LL144 AEDT | nyc.gov DCWP + NYS Comptroller audit 2025-12-02 + DLA Piper 2026-01-30 | Enforced 2023-07-05 | Do not expose practice scores as employment decisions; if ever employer-facing, require annual independent bias audit + public summary + 10-day notice | Practice `hire/no_hire` strings risk AEDT classification; enforcement intensifying after 17:1 detection gap |
| 7 | WCAG 2.2 AA | W3C TR/WAI quickref + webability.io 2026-07-21 | WCAG 2.2 (55 AA criteria) | Fix skip-link focus (`tabIndex=-1`), `aria-live` captions, 24px targets, no keyboard-only recording, visible focus not obscured | Recording via mouse/touch only; captions `pointer-events-none` unscollable, no `aria-live`; `icon-sm` ~32px |
| 8 | NIST AI RMF + GenAI Profile 600-1 | NIST AI 100-1 + AI 600-1 (July 2024), 12 GAI risks, Govern/Map/Measure/Manage | 2024-07-26 | Adopt Govern/Map/Measure/Manage + 12-risk catalog for evals (confabulation, bias, privacy, value-chain, human-AI over-reliance) | Current zero evals, zero model cards, zero risk register |
| 9 | Zhipu GLM API | docs.z.ai + therouter.ai 2026-06-14 + Cloudflare AI docs | 2026-08 / `open.bigmodel.cn/api/paas/v4`, `glm-4.7-flash` 200K, free-tier 1-concurrency | Keep OpenAI-compatible baseURL, centralize model IDs in `lib/zhipu.ts`, cap `max_tokens`/concurrency, server-side key only | IDs scattered across 13 files; `thinking+65536` unbounded; free-tier concurrency will throttle |
| 10 | Hiring fairness measurement | NYC LL144 FAQ (four-fifths rule 0.80, independent auditor, historical data, no imputed demographics) | 2026 | Any future employer scoring needs human labels + Kappa/Spearman/MAE + fairness drift tests without protected-trait profiling | Current scores have no agreement/stability/fairness tests |

Local doc notes (2026-09-03): `AGENTS.md:1-5` mandates reading `node_modules/next/dist/docs/` before code changes — obeyed for proxy decision (Context7 used because `node_modules/next/dist/docs` absent in installed 16.2.4 tree at that time; verified via official canary sources instead). `.env.example:6-15` only documents Zhipu/OpenAI/Supabase anon; no `service_role` guidance — intentional (never expose service_role to client).

## §B — 2026-09-13 refresh (what changed / what was confirmed locally)

Verified against installed tree: `next@16.2.4`, `react@19.2.4`, `ai@6.0.168`, `@ai-sdk/openai@3.x`, `@ai-sdk/google@3.x`, local docs `node_modules/next/dist/docs/{01-app/02-guides/upgrading/version-16.md,01-app/03-api-reference/03-file-conventions/proxy.md}` read in full; `node_modules/ai/dist/index.d.ts` inspected for `generateObject`/`Output` exports.

| # | Topic | Source | Date / Version | Decision for Interve AI (delta) | Reason |
|---|---|---|---|---|---|
| 11 | Next16 `proxy.ts` contract (local proof) | `node_modules/next/dist/docs/.../proxy.md:11,58-64,217-219,733-763` + `version-16.md:625-671` | 2026-09-13 / Next 16.2.4 installed | KEEP current `src/proxy.ts` named `export function proxy` (Phase 2 fix stands). Do NOT set `runtime:'edge'` in proxy (throws; proxy is nodejs-only, `version-16.md:629`). `export default` also accepted per `proxy.md:58` ("either as a default export or named proxy") — the Phase-1 "likely inert" warning was overstated, but named export is the documented forward path + codemod target (`middleware-to-proxy`) | Local doc explicitly allows both forms; edge-in-proxy unsupported; codemod `npx @next/codemod@canary middleware-to-proxy .` is the official migration |
| 12 | Next16 Async Request APIs + Turbopack + cacheComponents | `version-16.md:106-112,114-166,294-326,595-623` | 2026-09-13 / Next 16.2.4 | No code change needed: repo uses no sync `cookies()/headers()/params` in App Router (all AI routes use `Request` directly); `next.config.ts` keeps `turbopack.root` + `webpack.alias sharp/onnxruntime-node=false` — note `next build` now defaults to Turbopack and FAILS if a `webpack` key exists (`version-16.md:142`); our `webpack` key is tolerated today (build PASS) but migrate to `turbopack.resolveAlias` on next config touch. Do NOT enable `cacheComponents` without a Suspense audit (may surface uncached-data build errors) | Node>=20.9 required (`version-16.md:110`); local Node v24.15.0 OK; Docker `node:20` OK but pin engines. `next lint` removed (`version-16.md:1093`) — repo already uses `eslint` CLI directly (correct) |
| 13 | AI SDK v6 `generateObject` status (nuance correction) | Context7 `/vercel/ai` (`generate-object.ts`: `@deprecated Use generateText with output`), `migration-guide-7-0.mdx` (`experimental_output`→`output`), `node_modules/ai/dist/index.d.ts` (both `generateObject`+`output as Output` exported in `ai@6.0.168`) | 2026-09-13 / AI SDK 6.0.168 | `generateObject`/`streamObject` are **deprecated, NOT removed** in v6 — 10 route files still using them BUILD and PASS today. No emergency migration. Policy: new code uses `generateText`+`Output.object`; existing 10 routes migrate opportunistically via `npx @ai-sdk/codemod v6` + full regression (needs funded keys), tracked as tech debt, NOT a release blocker. The user-prompt claim "generateObject已废弃→must migrate now" is directionally right but overstates urgency for v6 | Telemetry docs confirm deprecated APIs emit the same spans; v6 codemods cover message renames, NOT a forced generateObject removal (removal is the v7 direction). Mass-migrating 10 routes without keyed regression would violate the no-unverified-rewrite iron law |
| 14 | Zhipu GLM latest ids + thinking (2026 refresh) | Web 2026-09-13: `z.ai/blog/glm-4.7` (2025-12-22), `docs.z.ai/guides/llm/glm-4.7`, HF `zai-org/GLM-4.7-Flash` (30B-A3B MoE, Interleaved/Preserved/Turn-level Thinking), `zai-org/GLM-V` (GLM-4.6V/4.5V; 2026-04-02 GLM-5V-Turbo notice) | 2026-09-13 / API `open.bigmodel.cn/api/paas/v4` unchanged | KEEP registry `MODEL_IDS` (`glm-4-flash` default, `glm-4.7-flash` thinking, `glm-4v-plus`/`glm-4v-flash` vision) — `glm-4.7-flash` confirmed as the current lightweight flagship; `thinking:{type:"enabled"}` + `max_tokens:65536` single-site `thinkingFetch` injection stands (was quadruplicated). Note: official thinking control is evolving toward `chat_template_kwargs.enable_thinking` / Turn-level Thinking — revisit when docs.z.ai publishes OpenAI-compat thinking params; do NOT invent new params from memory | BaseURL `https://open.bigmodel.cn/api/paas/v4/` confirmed current; free-tier 1-concurrency throttle risk stands → keep per-user limits + `FALLBACK_MAX_RETRIES=1`/`DEFAULT_MAX_RETRIES=2` |
| 15 | EU AI Act timeline correction (Omnibus) | `lumenova.ai/blog` 2026-09-08 + `ai-act-service-desk` Art.5/Annex III + `regumatrix.eu` Art.5(1)(f) + `ailawdecoded.com` 2026-04-15 | 2026-09-13 / Omnibus published 2026-07-24, in force 2026-07-27 | Art.5 bans (incl. workplace/education emotion inference Art.5(1)(f), €35M/7%) enforceable since **2025-02-02** — unchanged, our fake-vision removal + no-hire-verdict policy stays mandatory. Art.50 transparency (AI-interaction disclosure, synthetic marking, emotion/biometric notice) enforceable since **2026-08-02** — WE ARE NOW IN SCOPE: keep interviewer-AI disclosure + "training estimate, not an employment decision" labels (Phase 4 did this). Annex III high-risk hiring duties deferred to **2027-12-02** (Annex I 2028-08-02) — runway, not exemption. Legacy generative marking grace until 2026-12-02 | Original TECH_RESEARCH "hiring high-risk 2026-08" is STALE — corrected to 2027-12-02 per Digital Omnibus. Product positioning (practice-only, no employer decisions) remains the correct risk posture |
| 16 | OWASP LLM Top 10 2025 (confirmed) | `genai.owasp.org/llm-top-10` + PDF `OWASP-Top-10-for-LLMs-v2025.pdf` (v2.0, Nov 2024) | 2026-09-13 / v2025 (LLM01 Prompt Injection … LLM10 Unbounded Consumption) | Phase 12 `UNTRUSTED` fences on top-5 prompts + Zod I/O + rate limits + `data:image/*`-only vision (SSRF kill) stays. Remaining: LLM05 output-encoding for `message-card` markdown, strict CSP allowlist, Upstash multi-instance limits — tracked, not blockers | 2025 list expands Excessive Agency + Unbounded Consumption (cost/DoS) — directly maps to our per-user caps + concurrency guards |
| 17 | Supabase RLS (no doc change) | `supabase/migrations/002_session_ownership.sql:60-131` (this repo) vs Context7 RLS guide | 2026-09-13 | 002 design stands: `TO authenticated … ((select auth.uid())=user_id)` + contained `TO anon … (user_id IS NULL)`. Live RLS/IDOR tests still BLOCKED (no docker daemon / staging project in this env) — static contract tests pin the migration; live lane stays a release-tracked exception | No new Supabase breaking change found that invalidates 002 |
| 18 | React 19.2 / WCAG 2.2 / NIST RMF / NYC LL144 (reaffirmed) | `version-16.md:398-406` (React 19.2: View Transitions, useEffectEvent, Activity) + W3C WCAG 2.2 + NIST AI 600-1 + LL144 FAQ | 2026-09-13 | No new breaking change affecting this repo. Keep: keyboard-first recording toggle, skip-link focus transfer, `aria-pressed` toggles, 24px targets (measured), `Unavailable/Experimental` labeling, readiness-not-verdict, eval harness (Kappa/Spearman/MAE + stability + injection + fairness) | React Compiler (`reactCompiler:true`) NOT enabled — deliberate (build-perf data still being gathered upstream) |

## Local doc notes (2026-09-13)

- `AGENTS.md:1-5` obeyed: `proxy.md` (777 lines) + `version-16.md` (1237 lines) read from the installed 16.2.4 tree before any config change. No config change made in this refresh (read-only).
- `.env.example` now documents `SESSION_SECRET` (Phase 2) — the "naming confusion" H11 item (`OPENAI_BASE_URL` pointing at `bigmodel.cn`) is intentional OpenAI-compat wiring, documented in `src/ai/providers/registry.ts:59`; the real fix (centralized registry + server-only keys, no client reads) is done. `SESSION_SECRET` dev-fallback is loud + prod-fail-closed (`src/lib/api/session.ts:60`, `src/app/api/session/route.ts:53`).
- AI SDK: `npx @ai-sdk/codemod --dry v6` is the safe preview command; do NOT run write-mode without a funded-key regression lane.

## §C — 2026-09-14 refresh (live web search, V2 prompt basis)

> Date: 2026-09-14. Sources: nextjs.org/blog/next-16, ai-sdk.dev + vercel.com/docs/ai-sdk, supabase docs + supabase/agent-skills, genai.owasp.org LLM Top10 2025, euai-act.com + BrusselsTimes 2026-08-28 + DLA Piper draft guidelines. Priority: official > standards > papers > high-quality tech > blogs.

| # | Topic | Source / Date | Decision for Interve AI | Reason |
|---|---|---|---|---|
| 19 | Next.js 16 App Router 2026 | nextjs.org/blog/next-16; DEV 2026-04/06 guides | Turbopack default (keep `next dev --turbopack`); `params/searchParams` are Promise → must `await`; `cookies()/headers()/draftMode()` async-only; caching fresh-by-default → opt-in via `use cache` + `revalidateTag(tag, cacheLife)`; Node>=20.9, TS>=5.1, browsers Chrome111+/Safari16.4+; React Compiler stable (do NOT enable without perf data); `images.minimumCacheTTL` 60s→4h | Repo is Next 16.2.4: audit every `fetch()` for implicit-cache assumptions; keep Server-first, `use client` to leaves only; `practice/[id]` async-params pattern is the reference |
| 20 | AI SDK v6/v7 (`ai@6.0.168` installed) | ai-sdk.dev/docs `streamText/generateText`, vercel.com/docs/ai-sdk 2026-09-03 | New code: `generateText` + `Output.object` (Zod); keep existing 10× `generateObject` (deprecated, NOT removed, builds+passes); `streamText` for interview-chat SSE; unify via registry `resolveChat/Copilot/CostAware/Interview` + `withModelFallback`; explicit `maxRetries/timeout/abortSignal`; never trust client `model` param (allowlist only); log resolved `modelId` not raw spec | §B #13 stands: mass-migration without keyed regression violates no-unverified-rewrite law; `generateText+手工JSON.parse` lanes (trends/copilot) migrate to `generateObject`/`Output.object` opportunistically |
| 21 | Supabase RLS + perf | supabase docs RLS guide; supabase/agent-skills `security-rls-performance.md` | Owner pattern: `TO authenticated USING ((select auth.uid())=user_id) WITH CHECK (...)` per-op (never `FOR ALL USING(true)`); wrap `auth.uid()` in `SELECT` (100× on large tables); complex checks via `private` schema `SECURITY DEFINER` + `revoke execute` + index on `user_id`; per-table `supabase/tests/*_rls.test.sql` allow/deny + `supabase test db` in CI | 001's 7× `USING(true)` + 0001's no-RLS confirmed; 002 narrows to authenticated-owner + NULL-legacy anon bridge; live dual-user denial still pending staging |
| 22 | OWASP LLM Top10 2025 | genai.owasp.org 2025 (LLM01 Prompt Injection … LLM10 Unbounded Consumption); OWASP Top10 2025 A05 Injection | Treat resume/JD/transcript/code/image as UNTRUSTED (fences + ignore-instruction hardening, delimiter-escape); Zod I/O; encode LLM markdown output; least-privilege tool/function access + human approval for high-risk ops; cap tokens/cost per-user (Unbounded Consumption) | 5/14 builders fenced, 9 bare-interpolation builders must be fenced; `generateText+手工parse` coercion masks format errors; interview-chat is max attack surface (client-steerable fields) |
| 23 | EU AI Act hiring | euai-act.com hiring + high-risk requirements 2026-08-04; BrusselsTimes 2026-08-28; DLA Piper Art.6 draft guidelines; Reg 2026/1744 Omnibus (in force 2026-07-27) | Recruitment/filter/rank/score + video-interview analysis = Annex III 4(a) HIGH-RISK; Ch.III duties (Art.9 risk-mgmt, Art.10 data gov, Art.11 docs, Art.12 logging, Art.14 human oversight, Art.15 accuracy) apply from **2027-12-02** (runway, not exemption); since **2026-08-02**: transparency/AI-literacy enforceable + **workplace emotion-inference ban** (no face/voice/body → score); materially-influential output = high-risk even with human final decision; keep practice-only positioning + readiness-not-verdict + disclosure + human-overridable ranking + traceability | Direct basis for V2 iron laws: ban Face→emotion/personality/hireability, Culture Fit, Strong Hire/No Hire, protected-attribute inference; Practice Mode readiness labels + "training estimate" disclaimer; Privacy Center + MODEL/EVAL/DATA/RISK docs |
| 24 | Hiring fairness measurement | NYC LL144 FAQ; TECH_RESEARCH §A #6/#10 | Any future employer scoring needs annual independent bias audit + 4/5ths rule + public summary + 10-day notice; evals need human labels + Kappa/Spearman/MAE + stability + fairness-drift (no protected profiling) | Current V2 golden/synthetic + variance/injection/fairness lanes are the correct pre-requisite |

## §D — 2026-09-19 V5 Phase F1 refresh (local docs + implementation)

> Date: 2026-09-19. Installed: `next@16.3.5`, `react@19.2.4`. Local docs read: `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/{proxy.md:32,58, layout.md:8}`.

| # | Topic | Source / Date | Decision for Interve AI | Reason |
|---|---|---|---|---|
| 25 | `proxy.ts` contract (16.3.5 local proof) | `proxy.md:32` (named `export function proxy`), `proxy.md:58` (single function, default OR named), §B#11 | KEEP `src/proxy.ts:41` named export; NO `runtime:'edge'` (proxy nodejs-only); matcher keeps ` _next/static|image|favicon` exclusion | Named export is documented forward path + codemod target; edge-in-proxy unsupported |
| 26 | Dashboard Server-first split (F1-2) | `layout.md:8` (layout is outermost, wraps page; server layout may render client island; `children` passes RSC payload through) | `src/app/dashboard/layout.tsx` → server shell rendering `DashboardShell` island (`dashboard-shell.tsx`, `"use client"`); `useModalState` moved with island, `SessionDetailModal.tsx:12` re-pointed; `KnowledgeMatchLoader.tsx:1` given explicit `"use client"` (was implicit via client importer) | All dashboard data is browser-local (Dexie) + auth/modal/pathname hooks — island boundary is the honest minimum; server shell unblocks future per-page server-ification + metadata; zero behavior change (build+e2e guard) |
| 27 | F2 Compiler/forms verdict (no code change) | `next.config.ts:1-41` (no `reactCompiler` flag) + §B#18 + full-src grep 2026-09-19 | Compiler stays OFF (deliberate, §B#18 stands — no upstream perf data to overturn); manual-memo inventory (`memo` 11 files / `useMemo` 4 / `useCallback` 10) with ZERO deletions (iron law 8: no Profiler data); `use()` 0 usages (compliant by absence); `useActionState` pilot DECLINED — only 2 `<form>`s exist (`LoginForm.tsx:151`, `signup/page.tsx:97`, both auth-critical), settings controls are dead (no handlers), recruiter builder is not a form | Forcing the pilot would churn auth-critical flow or invent behavior (YAGNI); `<Activity/>` likewise declined (existing conditionals are mount/unmount semantics — modal scroll-lock, error auto-dismiss — Activity would alter semantics) |
| 28 | F3-split-1 single-mic slice (M5) | `vad.ts:1-5` ("Pure + tested; hooks own the WebAudio lifecycle") + M4/M5 structural identity + sign-off received 2026-09-19 | New `src/lib/audio/mic-analyser.ts` (`createMicAnalyser`: getUserMedia→ctx→source→analyser→idempotent dispose; throws to caller); `useAmbientNoise.ts` migrated (103→93 lines, same constraints/fftSize 512/smoothing 0.9/late-guard/catch-warn); RED→GREEN `tests/unit/mic-analyser.test.ts` (5 tests, stubGlobal mocks); M4/M1–M3 migration deferred to later slices | Honors established pure-helper architecture; M5 chosen (smallest, non-critical toast-only); e2e setup-leg 2× red proven unrelated via stash A/B (pass stashed + pass restored = timing flake, §6 variance only) |
| 29 | F3-split-2 VAD migration (M4) | `useVADInterruption.ts` pre-shape (generation guard + vadStep + callback ref) | Same helper: `createMicAnalyser({ deviceId, smoothingTimeConstant: 0.4 })` (fast-attack rationale kept as comment); generation guard, vadStep machine, `onInterruptRef`, catch text unchanged; `micConstraints` import dropped from hook (lives in helper) | M4/M5 lifecycles now identical-shape via one module; remaining M1–M3 handoff convergence is behavioral (needs human re-sign per T2.5–T2.7), not structural |
| 30 | B1-1 owner-id write binding (005 NOT generated) | `api-client.ts:60-307` (single write funnel: 7 inserts/upsert + 2 updates) + `rls-policies.test.ts` H2.1 pin (005 stays ungenerated until live denial) + OAuth bridge (`AuthContext.tsx:57-70`, `su.id` = auth.uid) | New `stampOwner`/`currentOwnerId` in `api-client.ts`: stamp `user_id` = session uid when present, leave NULL-bridged for demo logins, never overwrite caller-bound ids; wired at all 9 write sites; reads untouched; RED→GREEN `owner-binding.test.ts` (4 tests, mocked supabase) | 005 would brick demo remote writes (demo logins have no JWT) → stays ungenerated per pinned precondition + external ④; binding alone is safe (demo path byte-identical, OAuth path goes NULL-invisible → owned) but OAuth live behavior still needs staging denial to observe |
| 31 | B2 query discipline (guardrail, no blind trim) | 13× `select('*')` all in `api-client.ts` (single read funnel; `db.ts:182` routes ALL app reads through it) + consumer reads (`dashboard/page`, `SessionDetailModal` full-row dossier, recruiter, telemetry) + zero REFERENCES in 0001/001 | New `select-discipline.test.ts`: count-pin 13 + per-site justification + telemetry-paged guard. Whitelisting DECLINED per site: list readers need full rows (modal/dossier reuse list objects incl. resumeText/councilDebate/timeline), `toCamelCase` casts hide missing columns (silent-undefined risk), no live DB to verify savings. N+1 audit CLEAN (one query + in-memory joins everywhere). FK audit: no FKs (app-joined); 001/002 indexes cover status/created/owner/category/endpoint/timestamp/code; pg_stat_statements additions need live DB (deferred per V5) | Correctness > bytes without measurement; trim + modal lazy-get tracked as future slice requiring reader-evidence + SELECT-遗漏断言 (§4.7) |
| 32 | B3 cache layering (zero domains + guardrail) | Full-src grep 2026-09-19: zero `'use cache'`/`cacheLife`/`cacheTag`/`revalidateTag`/`updateTag`, zero `"use server"`, no `cacheComponents` in `next.config.ts` | New `cache-discipline.test.ts` (3 tests): future `use cache` must co-carry cacheLife+cacheTag (law 10), no user-level identifiers in cached domains, cacheComponents stays off without Suspense audit (§B#12). Zero domains is CORRECT: hot paths are client-side (Dexie/Orama via api-client funnel — `use cache` is server-only), all 19 Route Handlers personalized (law 10 forbids), static data already free (question-bank SSG, i18n imports), Redis declined per V5 | Compliant-by-posture + tripwire; revisit only when a shared-slow server read appears with transfer evidence |
| 33 | B4 observability + survival (verify-standing, no code change) | `health/route.ts`, `csp-report/route.ts`, `logging.ts:5-16,57-64` (input/output split, PII-free), `proxy-guard.test.ts:81-83` (CVE-2025-29927), `degradation-matrix.test.ts` (500/429/timeout→fallback→422→mock→typed error; cache tier deferred with basis), `guard.ts:54-55,75,119` (RateLimit-*/Retry-After), `user-budget.ts`, quota ledger | Executed 2026-09-19: `verify:supabase-free` PASS (dotenv fill works; anon sees only NULL-bridge rows by 002 design), `npm audit --audit-level=critical` exit 0, C1 ledger zhipu 0/50 + gemini 0/20 (this session: 0 keyed). No code change — every B4 line item was already standing and re-verified by execution | csp-report 周检 + live dual-user denial stay external/time-based (④); per-user budget + upload chain covered by standing tests (all green in 45/350) |
| 34 | A1-1 generateObject→Output.object mapping (code change NOT started) | Installed `ai@6.0.168` `index.d.ts:1362` (generateText accepts `output`+`stopWhen`+`maxRetries`+`abortSignal`), `:2911-2923` (`Output.object({schema,name?,description?})` — NO repairText), `:6015-6029` (`extractJsonMiddleware({transform})`), `registry.ts:193-217` (Zhipu ignores json_schema, live-verified; `repairZhipuJson` exists for it); `codemod v6 --dry` = no-op (no auto-migration for deprecated→Output) | 10 routes pinned (`analyze-match/practice/code/chunk/behavior`, `parse-jd`, `analyze-interview/alignment`, `init-context`, `analyze-star`); first target `analyze-match` (67 lines, single call). REPAIR GAP: migration drops `experimental_repairText` → Zhipu fenced outputs flip repaired-200 → NoObjectGeneratedError-500 unless `extractJsonMiddleware({transform: stripJsonFences-ish})` + wrapLanguageModel is added (registry-level, broad blast radius — decision needed). Per-route keyed MVVP required before/after (V5) → quota approval gate | Do NOT migrate without keyed regression; E4 `no_object_generated` re-attribution rides on the same before/after pair. **OUTCOME 2026-09-19 (3/3 approved calls spent): migrated analyze-match to generateText+safeParse (Output.object repair V3-blocked, local d.ts proof) → AFTER 500 vs BEFORE 200; diag = schema-fail on equivalent content (constraint-loss mechanism, sampling not excludable) → REVERTED one file (E4 precedent); guards restored; see EVAL_REPORT §5.2 A1 entry. Next unblock: provider-SDK major bump.** |
| 35 | G1 deploy-consistency (static pins, no spend) | `vercel.json` (installCommand only), `Dockerfile` (node:20-alpine multi-stage, non-root nextjs, standalone copies), `robots.ts` (practice-only surface), `I18N_POLICY.md` (EN branch gated on keyed MVVP) | New `deploy-consistency.test.ts` (3 tests): vercel×standalone non-conflict, Dockerfile-base×engines-node-20 triple pin, robots allow/disallow pin + no-sitemap-field. DECLINED with basis: Dockerfile HEALTHCHECK (no daemon in this env — §B#17 — unverifiable, tracked), sitemap/metadataBase/OG/JSON-LD (no prod domain; V5 order is domain-first), CSP enforcing (needs report-stream traffic), i18n EN branch (keyed MVVP per policy), tag/release sign-off (human) | G shippable remainder is external-gated (domain, daemon, sign-off); revisit on domain day |
| 28 | OpenRouter free third lane (live-verified) | `openrouter.ai/api/v1/models` 2026-09-19 (446 models, 22 `:free`) + per-model `supported_parameters`/`architecture.modality` + 2 live smoke calls (`cost: 0` both) | Registry-only integration: `openrouter()` singleton (OpenAI-compat baseURL, server-only key, `HTTP-Referer`/`X-Title`, `.chat()` only) + 2 MODEL_IDS + 2 opt-in specs (`openrouter` → Nemotron Ultra free-form; `openrouter-structured` → DeepSeek structured candidate); 19 routes + fallback chain + defaults UNTOUCHED; ultra BANNED from generateObject lanes (text-only modality, NO `response_format`); vision gap stays on `glm-4v` (no free-ultra vision; free vision candidates `gemma-4-31b`/`nex-n2.5-pro` noted, not wired); free rotation + rate-limit + third-party routing → NEVER default, resume/transcript stay off OpenRouter (Privacy Center note pending) | Ultra smoke leaked reasoning under a 20-token cap (reasoning default high, 22 reasoning tokens) → `reasoning:{effort/exclude}` tuning deferred to Phase A; interview-chat sets no `max_tokens` so unaffected; token accounting needs no change (`usageOf` reads standard AI SDK shape; interview-chat logs spec string `openrouter`, trend-separable) |
| 32 | Guard timeout unenforced on edge routes (root-caused, zero keyed calls) | `node_modules/next/dist/compiled/edge-runtime/index.js` (class `A`: `static abort` + `static timeout`, NO `static any`) + `src/lib/api/guard.ts:43-50` + AI SDK v6 `generateObject` source (`abortSignal` forwarded into `doGenerate`, threaded through `prepareRetries`) | `combineSignal` ALWAYS takes `return client` on edge routes → `timeoutMs` (default 25s) silently unenforced on all 19 edge AI routes; only client disconnect aborts. Explains MVVP's 39–60s 200s. Fix (NOT applied): manual two-signal composition replacing the `AbortSignal.any` dependency — small, keylessly testable, no route changes. Caveat: Vercel workerd HAS `AbortSignal.any`, so dev/prod diverge — verify on production build before claiming prod impact | SDK side correctly plumbed (signal forwarded, retry wrapper aware) — fault is the missing `any` static in the sandbox ponyfill, not our wiring or the SDK; keyless gates never exercise this (no timer elapses in tests), which is why it hid |
