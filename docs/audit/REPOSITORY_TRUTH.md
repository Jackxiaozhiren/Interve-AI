# REPOSITORY_TRUTH (HEAD `f37f574` + working tree, 2026-09-13)

> Iron-law source: working tree inspected directly 2026-09-13. HEAD is `f37f5747396eb8cc6dfdc5296a8f64a3d11b1f7b` on `main`; the working tree contains the uncommitted Phase 2-13 train (docs, `src/ai/`, `src/lib/api/`, `002` migration, 196-test gate). Where HEAD and working tree differ, BOTH are stated. README/commit/history claims are corrected in §10, not trusted.
> Companion: `MASTER_AUDIT.md` (Phase 1 read-only) + `../research/TECH_RESEARCH.md` (§B refresh).

## 1. HEAD / toolchain

| Item | Truth |
|---|---|
| HEAD SHA / branch | `f37f5747396eb8cc6dfdc5296a8f64a3d11b1f7b` / `main` (`git log --oneline -15` head) |
| Package manager | npm (`package-lock.json` present; `vercel.json: installCommand: npm install`; `Dockerfile` supports npm ci). No `pnpm-lock.yaml`/`yarn.lock` active. Local `node v24.15.0`, `npm 11.12.1`; Docker `node:20-alpine`; Next requires Node>=20.9 — all compatible, but `engines` is unpinned (tracked) |
| Framework / UI | `next@16.2.4`, `react@19.2.4` + `react-dom@19.2.4`, `@types/react@19`, TS `strict:true`, `eslint@9` + `eslint-config-next@16.2.4` (flat config), `tailwindcss@4`, `shadcn@4.3.0` |
| AI | `ai@6.0.168`, `@ai-sdk/openai@3.x`, `@ai-sdk/google@3.x`, `@ai-sdk/react@3.0.170`, `zod@4.3.6` |
| Data / state | `@supabase/supabase-js@2.104.1`, `dexie@4.4.2` (orphan dep, tracked), `zustand@5.0.12`, `@orama/orama@3.1.18` |
| Media / editors / viz | `tldraw@4.5.10` (+`@tldraw/tldraw`), `@monaco-editor/react@4.7.0`, `@huggingface/transformers@4.1.0`, `kokoro-js@1.2.1`, `recharts@3.8.1`, `mermaid@11.14.0`, `html2canvas@1.4.1`, `jspdf@4.2.1`, `pdf-parse@2.4.5` (+`pdfjs-dist` REMOVED Phase 12), `framer-motion@12`, PWA `@ducanh2912/next-pwa@10.2.9` |
| Scripts (package.json) | `dev (next dev --turbopack)`, `build`, `start`, `lint (eslint)`, `typecheck (tsc --noEmit)`, `test (vitest unit+integration)`, `test:unit`, `test:integration`, `test:e2e (playwright test)`, `test:e2e:mock`, `test:eval (vitest.eval.config)`, `test:e2e:smoke`, `test:a11y`, `verify (lint+typecheck+test+build)` |
| Gate status 2026-09-13 | `lint` 0/0 · `typecheck` PASS · `test` **196/196** (24 files) · `build` PASS (37 routes: 17 API + pages; chart `-1` width warnings only) — re-run this session |

## 2. AI providers & model-id table (single source: `src/ai/providers/registry.ts:15-25`)

| Canonical key | Model id | Vendor / path | Used for |
|---|---|---|---|
| `zhipuFlash` | `glm-4-flash` | Zhipu BigModel, OpenAI-compat `https://open.bigmodel.cn/api/paas/v4/` | Default interviews, evals, copilot default, fallback tier |
| `zhipuThinking` | `glm-4.7-flash` | Zhipu, `thinkingFetch` injects `thinking:{type:"enabled"}` + `max_tokens:65536` server-side | Long/complex interviews + evaluations (`resolveCostAwareModel` >2000 chars, `resolveInterviewModel` >8 turns) |
| `zhipuVision` / `zhipuVisionFlash` | `glm-4v-plus` / `glm-4v-flash` | Zhipu vision | Whiteboard/diagram review, document OCR fallback (`data:image/*` only — remote-URL SSRF refused) |
| `geminiFlash` | `gemini-2.5-flash` | Google AI (`@ai-sdk/google`) | Alignment, code review, practice grading, JD parsing (canonical id without `models/` prefix) |
| `geminiPro15` / `geminiFlash15` | `gemini-1.5-pro` / `gemini-1.5-flash` | Google AI | Legacy interview-chat/copilot fallbacks (compat) |
| `gpt4o` / `gpt4oMini` | `gpt-4o` / `gpt-4o-mini` | OpenAI (optional user key) | User-selected `?model=openai` fallback only; absent by default |

Construction: ONE site per vendor (`zhipu()/google()/openai()` singletons, `resetProviderClients` test hook); ONE `thinkingFetch`; `DEFAULT_MAX_RETRIES=2`, `FALLBACK_MAX_RETRIES=1`; `resolveChatModel/resolveCopilotModel/resolveCostAwareModel/resolveInterviewModel` replace triplicated routing. Static guard `tests/unit/ai-registry.test.ts` bans `createOpenAI(`, direct `@ai-sdk/google` imports, hardcoded ids, mock-hook skips — 16/16 routes clean. Mock tier `AI_MOCK=1` (prod-disabled, post-gate, schema-validated) + `withModelFallback` (`x-fallback` marking). Prompts: versioned registry `src/ai/prompts/` (17 entries incl. `interview-coverage 1.0.0`).

## 3. DB / Auth / State / PWA

- **DB**: Supabase Postgres, 7 user tables (`interviews, evaluations, practice_sessions, telemetry, achievements, orama_index, assessments`). `0001_initial_schema.sql` (UUID, JSONB, no RLS/indexes) vs `001_init_schema.sql` (BIGSERIAL, TEXT[], defaults+CHECKs+11 indexes, 7× open policies) — BOTH frozen as history. `002_session_ownership.sql` (additive, idempotent) adds nullable `user_id UUID` ×7 + owner indexes ×7, drops the 7 open policies, creates `Owner full access (TO authenticated, (select auth.uid())=user_id)` + contained `Legacy anon unowned access (TO anon, user_id IS NULL)`. Zero FKs (tracked). Frontend `lib/api-client.ts` (Dexie-emulation) + `lib/supabase.ts` anon singleton; `lib/db.ts` alias.
- **Auth**: transitional (documented, not hidden). HMAC-signed HttpOnly server cookie `{v:1,id,email,username,iat,exp/24h}` (`src/lib/api/session.ts`, WebCrypto edge-safe); `POST/GET/DELETE /api/session` (Zod, 30/min); `AuthContext` authority = server cookie (localStorage UI-only); `src/proxy.ts` named `export function proxy` verifies signatures, protects `/dashboard /setup /interview /chat /recruiter /practice` + `/api/dashboard`, redirects anon to `/login?from=`, 401s JSON for `/api/*`. Legacy unsigned cookies fail closed (one re-login). Prod without `SESSION_SECRET≥16` fails closed; dev loud fallback. Full Supabase Auth cutover + live RLS/IDOR tests BLOCKED (no staging project/docker in this env) — static contract `tests/integration/rls-policies.test.ts` pins 002.
- **State**: `useInterveStore` (interview content) + `useAppStore` overlap (resume/JD/cheatsheet — tracked P2) + `useInterviewLoopStore` (Phase 6 loop: difficulty/turns/timing/questionHistory caps) + `useAccessibilityStore` (persisted Calm/LiveCaptions/Dyslexia + `showLiveInsights` default OFF) + `AuthContext`. Orama search warn-only, Dexie orphan.
- **PWA** (`next.config.ts:4-7`): `@ducanh2912/next-pwa`, `dest:public`, `disable: dev`. `start_url:/dashboard` (login-flash tracked), NO `runtimeCaching` (Whisper/Kokoro hundreds of MB uncached — tracked), no offline fallback/screenshots/shortcuts. `output:standalone`, `serverExternalPackages:[pdf-parse]`, browser aliases `sharp/onnxruntime-node=false`.

## 4. Audio (STT/TTS/VAD/barge-in) — `src/lib/audio/`, `src/hooks/`, `src/workers/`, `AUDIO_REPORT.md`

Triple-stream topology (recorder + VAD + ambient, documented cost/echo) + `echoCancellation/noiseSuppression/autoGainControl` everywhere, persisted device select, GreenRoom retry (was fatal). MediaRecorder: per-recording track release + unmount teardown (`unmountedRef`). STT: `SttSession` (partials/finals/confidence/lang-mix/reconnects; bounded 3× auto-restart on `network` only). VAD/barge-in: pure `vadStep` + generation guard, trigger counted as `interruptions` (same 25/5 thresholds). TTS: Kokoro-82M-ONNX-q8 (`af_heart`, EN) / OS voices (ZH); stops on barge-in. Delivery analytics persisted (`wpm, fillerWords, interruptions?, avgAnswerSec?, avgRoundTripMs?, sttAvgConfidence?, sttFinals?, sttReconnects?`, additive/optional; `summarizeDelivery` returns nulls, never guesses; unmeasured renders "—"). Single-shared-stream refactor + `pauseRatio` + keyed TTS-latency breakdown deliberately deferred (need real-device CI). No `Math.random`, no bare `{audio:true}` remain in audio paths (grep-verified).

## 5. Vision — self-view vs diagram/whiteboard/code

Camera = **optional self-view only** (`CameraSelfView.tsx`: preview + permission states, "Local only / No visual analysis", zero metrics, zero callbacks). Deleted: `VisionTelemetry.tsx` (`Math.random()` eye/posture/expression → `bodyLanguageScore` → `radarScores`), `TelemetryWidget` focus tile, `SystemHealthIndicator` fake latency, `LiveStats` vision tile, per-chunk `analyze-chunk` quota burn, `interview-chat` cognitive-load pressure branches. Real vision = `analyze-vision` (`glm-4v-plus`, tldraw snapshot / document / chart, `data:image/*` only). Doctrine (binding): Language = content/STAR/correctness/evidence; Audio = observable delivery only (rate/pause/filler/latency/duration/volume-stability); Visual = diagrams/whiteboard/code/docs/charts first. BANNED: face→emotion/personality/honesty/intelligence/hireability/culture-fit; accent/voice→competence; any protected-attribute inference. Unmeasurable → `Unavailable/Experimental`, never a percentage.

## 6. Resume + JD parsing

`POST /api/parse-resume` (5MB server cap, SVG refusal, OCR text cap 60k, `pdf-parse` server-only; dead `lib/pdf-parser.ts` + `pdfjs-dist` HIGH removed Phase 12) + `POST /api/parse-jd` (`questionCount` clamped 1-20). Both session-gated, Zod-validated, byte-capped, rate-limited, mocked (`AI_MOCK`). `setup/page.tsx` wizard persists to interview record; JD no longer auto-pollutes (Phase 2 fix); `?context=` URL JD leak closed (POST/session). Upload abuse tests: 413 (JSON/5MB/SVG/content-length lie), 400 (MIME). OCR exfil consent + single-pipeline unification tracked.

## 7. Scoring chain (V2, evidence-grounded)

`analyze-interview` (guard/rate-limit/cost-routing/fallback/observability preserved): rubric selection → `src/ai/prompts/evaluation.ts` (anchors inlined, lower-on-tie, no-evidence→1, forbidden list, STAR-marking, Chinese output) → `EvaluationV2Schema` → server normalization (pins version/rubricId, drops off-rubric dims, tolerates partial). Rubrics `src/ai/rubrics/` (5 versioned: behavioral/technical/system-design/data-ml/general-v1, 4-6 dims each, 1-5 anchors). Every dimension REQUIRES ≥1 verbatim evidence quote (Zod `.min(1)`), 1-5 anchor levels (0-100 = ×20 derivation), per-dim evaluator `confidence` high/medium/low (evidence sufficiency, never candidate psychology), overall = weakest link, `improvement` drill/dim, `strengths/weaknesses/nextDrills` (feeds drills). Readiness `needs_foundation/developing/interview_ready/strongly_prepared` + rationale + mandatory disclaimer ("Training estimate … not an employment decision"). NO `hireVerdict` generation, NO `cultureFitAdvisor`/`culturalTraits` generation (legacy fields read-only via `src/lib/eval-compat.ts`: V2 full view / legacy transparency view / empty none). Legacy UI sections (`report`, `SessionDetailModal`, `PrintableDossier`, `PrintLayout`, `achievements`, `analyze-trends` fallback) render historical rows with captions — generation is gone, display of history is fidelity. Calibration (golden/human Kappa/Spearman/MAE, variance) is the keyed eval lane (`evals/`, `npm run test:eval`).

## 8. Dashboard / Replay / Drills / Coding / Whiteboard

- **Setup** (5 steps + preflight GreenRoom + device picker + plan preview): goal-based (11 interview types + custom, 4 difficulties, 10/15/20/30-min budgets); type→rubric mapping; coding auto-enables scratchpad; plan (`interview/plan.ts`: alignment strengths/gaps labeled AI-estimated, unknowns stay `unknown`, gaps-first focus ≤3 + rubric fill ≤4) persisted + travels via URL into the loop.
- **Interview loop** (`src/ai/interview/state.ts` + `prompts/coverage.ts` + `useInterviewLoopStore` + `interview-chat`): server rebuilds `InterviewState v1` (difficulty/turns/budget/questionHistory≤50/STAR-trait avgs/followUpTargets≤5) from `messages` every turn; `difficultyHint` only seeds ±1 adaptation (easy..expert, hold if <2 turns); coverage section (turn/difficulty/budget/avgs/prioritized gaps/last-3 anti-repeat/wrap-up <20%); stealth-probing block deleted; staleness fix (`sendMessage({body:fresh})` overrides render-time snapshot); short-answer probe (<120 chars → ask for example/number); UI badge `Turn N · Difficulty`.
- **Dashboard**: history (keyboard-operable cards), badges/scores/radar (`sessionScore()` unified V2-first), `ProgressInsights` (4-wk frequency, type performance, STAR completion, depth, length, velocity — pure, nulls "—"), trends (V2-first, legacy fallback), achievements (V2 logic + grandfathered codes).
- **Report/Replay**: readiness header + rationale + radar + dimensions + drill plan (weakest-first, task+tip+practice link); replay per-turn retry deep links (`/practice?retry=`, unicode-safe) + Diagnostics/Dimensions/Readiness tabs; per-turn rubric persistence + generated "Alternative Approach" deferred (needs keys+eval); `perfectRewrite` ("Better Answer") exists.
- **Drills**: 28 curated (one/dim, versioned) + `drillsForWeaknesses` (weakest-first ≤3); practice hub custom-retry sessions reuse `PracticeSessionClient` (stable per-question ids), attempts history with deltas (Retry→Compare), `?q=` prefill.
- **Coding**: Monaco JS-only (multi-language execution via Judge0/Pyodide deferred — new infra + sandbox review); `analyze-code` (cached `s-maxage=3600`), staged hints, AI review (correctness/complexity/reasoning/tests/quality/expression).
- **Whiteboard**: tldraw snapshot review via vision (per-stroke continuous eval deferred — model spend + UX); `persistenceKey` namespacing tracked.

## 9. Test matrix + deploy

| Lane | Command | 2026-09-13 result |
|---|---|---|
| Lint | `npm run lint` | 0 errors, 0 warnings |
| Typecheck | `npm run typecheck` | PASS |
| Unit | `npm run test:unit` | 16 files PASS (part of 196) |
| Integration | `npm run test:integration` | 8 files PASS (incl. `api-hardening` 27, `rls-policies`, `truthfulness`, `prohibitions`, `mock-contract`, `eval-datasets`, `interview-loop`, `proxy-guard`) |
| All | `npm run test` | **24 files, 196/196 PASS**, no network/keys |
| AI eval | `npm run test:eval` | keyless 4 skipped exit 0 (keyed: golden MAE<1.0/adjacent>0.8/ρ>0.5/κ>0.4 provisional + 3-repeat stability + injection drift≤20 + fairness drift≤20) |
| E2E smoke | `test:e2e:smoke` (chrome-1280x720, 9 specs) | last full run 17 passed/3 skipped (chat fixme needs keys); re-ran touched lanes at each gate |
| a11y | `test:a11y` + `keyboard.spec.ts` (3/3) + axe lane | skip-link focus transfer, mic keyboard toggle, insights default-off verified |
| Build | `npm run build` | PASS, 37 routes |
| Verify | `npm run verify` | PASS (lint+typecheck+test+build) |
| Security | `npm audit` | 39 (1L/30M/8H/0C) post-`audit fix` + triage (none reachable; upgrade lane Next 16.3.4); secret scan 0 hits; gateway/upload/abuse suites green |
| Perf | Lighthouse + `scripts/perf-probe.mjs` | `/` 92/100/100/100 (meets all); `/landing` 86, `/login` 89, `/signup` 88 Perf (decor-LCP driver, documented); guarded pages FCP 92-184ms, CLS 0, TBT 0 |

Deploy: `vercel.json` (npm install) + `Dockerfile` (node:20-alpine multi-stage, npm ci) + `output:standalone`. No `.github/` CI in HEAD (added §11). No `engines` pin (tracked).

## 10. README-vs-reality diff (README is stale — rewrite in §11)

HEAD `README.md:1-36` is unmodified `create-next-app` boilerplate (`app/page.tsx`, Geist, `next/font`, Vercel link). Reality: a 182-file interview-practice platform (Setup→GreenRoom→Interview/Coding/Whiteboard→Analysis→Replay→Drills→Dashboard→Privacy/Settings) with 17 API routes, Zhipu/Gemini/OpenAI routing, Supabase 7-table schema + 002 ownership migration, signed sessions + proxy guard, evidence-grounded V2 evals, 196-test gate, PWA shell, local Whisper/Kokoro workers. The README mentions NONE of this (no screenshots/architecture/features/stack/env/AI/Supabase/migrations/tests/security/privacy/ResponsibleAI/deploy/roadmap/limitations). Clean-room `git clone && npm ci && cp .env.example .env.local && npm run verify && npm run dev` works keyless (AI routes 401 without session; mock tier `AI_MOCK=1` for handler tests) but is UNDOCUMENTED until the §11 rewrite.

## 11. H1-H11 independent re-verification (evidence + file:line, no preset conclusions)

- **H1 "README = create-next-app default?" — CONFIRMED.** `README.md:1` "This is a Next.js project bootstrapped with create-next-app", `:19` "modifying app/page.tsx", `:32-36` Vercel boilerplate. The repo has no `app/page.tsx` tutorial content relationship to the actual `src/app/{landing,setup,interview,dashboard,practice,chat,recruiter}/` product. Fix: §11 rewrite (done in this release).
- **H2 "`npm ci && lint && tsc && build` real error counts?" — MEASURED 2026-09-13.** `npm run lint` 0e/0w; `npx tsc --noEmit` 0; `npm run build` PASS (37 routes, only recharts `-1` size warnings); `npm run test` 196/196; `npm audit` 39 (1L/30M/8H/0C). Phase-1 baseline (fresh `npm ci`: 0e/13w, tsc PASS, build PASS, 62 vulns) is reconciled: warnings eliminated, vulns reduced via `audit fix` + `pdfjs-dist` removal.
- **H3 "TelemetryWidget/LiveStats/analyze-vision package eyeContact/posture/emotion/confidence as real AI?" — FIXED, verified absent.** `grep Math.random src/components/interview src/app/interview` = 0 hits (2026-09-13). Decorative `Math.random` remains ONLY in `landing-background.tsx:43-48` (canvas particles) + `toast.tsx:33` (id) + dead `lib/auth-context.tsx:55` (unwired mock) — none scored/stored. `VisionTelemetry.tsx` DELETED; `CameraSelfView.tsx` has zero metrics/callbacks; `TelemetryWidget` focus tile removed; `SystemHealthIndicator` latency number removed (`wsLatency: never` type guard); `LiveStats` vision tile deleted, Load→"Strain · Experimental" with non-medical caption; per-chunk `analyze-chunk` call + `cognitiveLoad` pressure branches removed (`truthfulness.test.ts` 8 asserts). No P0 truthfulness defect remains in the live path. `analyze-vision` scores ONLY diagrams/documents/charts (snapshot-scoped), never faces.
- **H4 "analyze-interview/behavior/star output 0-100 + Culture Fit/Hire-NoHire without rubric/evidence/uncertainty?" — GENERATION FIXED, HISTORY DISPLAY REMAINS (by design).** `analyze-interview/route.ts:12-13,75-101` now outputs `EvaluationV2Schema` only (no `hireVerdict`/`cultureFitAdvisor` generation); `evaluation-contract.ts` enforces ≥1 verbatim evidence/dim, 1-5 anchors, per-dim + overall confidence, improvement drills; `rubrics/` provides 5 anchored rubrics. `grep hireVerdict|cultureFit|culturalTraits` still hits legacy DISPLAY paths (`report/[id]:134,321-323,391-400,602`, `SessionDetailModal:198-228`, `PrintableDossier:107-120`, `PrintLayout:65-98`, `achievements:160-174`, `analyze-trends:48-64` fallback, `eval-compat.ts:4`) — all read-only fidelity for pre-V2 rows with 历史评估 captions, covered by `eval-compat` tests. No new hire/culture verdict is generated. `analyze-behavior/star/chunk` still output 0-100 via `generateObject` without V2 envelopes — tracked as the remaining calibration debt (keyed golden/Kappa lane owns it).
- **H5 "0001 vs 001 UUID/BIGSERIAL/TEXT[]/JSONB/RLS/index drift?" — CONFIRMED, reconciled without rewriting history.** `0001:4,31…` UUID + JSONB, no defaults/RLS/indexes vs `001:20,57…` BIGSERIAL + TEXT[] + defaults/CHECKs + 11 indexes + triggers; frontend `Number(id)/parseInt` (`report/[id]:95`, `replay/[id]:35`) assumes numeric ids. `002_session_ownership.sql` is additive/idempotent (nullable `user_id`, owner indexes, policy swap) with rollback header; both initials frozen. Fresh-`db reset` live verification still needs a staging project (tracked).
- **H6 "001 FOR ALL USING(true) + 002 anon FOR ALL privilege escalation?" — 001 CONFIRMED OVER-PERMISSIVE; 002 CONTAINED (not escalation).** `001:203-221` 7× `FOR ALL USING(true) WITH CHECK(true)` with no `TO` restriction (anon-readable/writable) + zero `user_id` cols (`rls-policies.test.ts` pins this as the pre-002 fact). `002:61-94` `FOR ALL TO authenticated … auth.uid()=user_id` (owned rows) + `002:98-131` `FOR ALL TO anon … user_id IS NULL` (legacy unowned only; owned rows invisible to anon). The anon policy NARROWS the 001 exposure (NULL-gated) as a demo-continuity bridge until the Auth cutover backfills `user_id`; it is not a new grant on owned data. Residual: legacy NULL rows remain anon-accessible by design until cutover — documented exception, live RLS/IDOR tests pending staging.
- **H7 "Unit/integration/API/RLS/AI-eval/E2E/a11y/security + CI truly run?" — MOSTLY TRUE, two gaps closed in §11.** Unit (16) + integration (8) + API hardening (401/400/413/429/session matrices) + RLS static + AI-eval keyless (datasets + metrics) + E2E smoke (chrome-1280x720) + a11y/keyboard + security (audit/scan/gateway/upload) all run and green (see §9). Gaps found: (a) NO `.github/` CI in HEAD or tree (verified `ls .github` → missing) — added `.github/workflows/ci.yml` (install→lint→typecheck→unit→integration→build→e2e-smoke→audit+eval-smoke, PR red-blocks) in this release; (b) `npm run verify` omits e2e/a11y by design (speed) — documented, CI runs them.
- **H8 "AI SDK v6 deprecated-API residue count?" — 10 files, NON-BLOCKING (nuance).** `grep -rl generateObject|streamObject src/app/api` = 10 (`init-context, alignment, parse-jd, practice, star, interview, behavior, chunk, code, match`). `ai@6.0.168` still exports both (`index.d.ts`) with `@deprecated Use generateText with output` on `generateObject`; telemetry parity documented. `generateText` already used in `generate-hint, copilot, analyze-vision, analyze-trends, interview-chat(streamText), parse-resume(OCR)`. Decision: residue stays for v6 (builds + passes), migrates via `npx @ai-sdk/codemod v6 --dry` + keyed regression (tracked). Static guard prevents NEW direct-client/hardcoded-id drift.
- **H9 "Zhipu model ids + thinking hack expired?" — CURRENT, centralized.** `registry.ts:15-25` ids (`glm-4-flash/4.7-flash/4v-plus/4v-flash`, `gemini-2.5-flash/1.5-pro/1.5-flash`, `gpt-4o/mini`) match 2026-09-13 web truth (GLM-4.7 flagship, 4.7-flash lightweight; baseURL `open.bigmodel.cn/api/paas/v4` unchanged). Quadruplicated `thinking+65536` hack deduplicated to ONE `thinkingFetch` (thinking-model-only). `compatibility:"compatible"` `@ts-expect-error` retained (single site). Gemini `models/`-prefix canonicalization verified by docs, pending keyed proof (documented). No expired id found.
- **H10 "tldraw/Monaco/transformers/kokoro bundle + PWA offline risk?" — CONFIRMED RISK, partially mitigated.** tldraw (~1MB+) + scratchpad now `next/dynamic ssr:false` drawer-gated (interview transfer 1032KB, PERF_REPORT); `sharp/onnxruntime-node` aliased out of browser; Whisper-base + Kokoro-82M still hundreds of MB with NO `runtimeCaching`/offline fallback (`next.config.ts` has no `runtimeCaching`; PWA `disable:dev` blocks dev verification). Dashboard `Perf 78` (LCP 6.0s, decor-blur driver) vs `/` 92/100/100/100. Mitigation tracked (static decor snapshot, per-route decor opt-out, provider-chain deferral, Upstash limits); not a ship-blocker for practice-only launch with documented budgets.
- **H11 ".env naming chaos (OPENAI_BASE_URL→bigmodel.cn) + SESSION_SECRET fallback unsafe?" — EXPLAINED, hardened.** `OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4/` (`.env.example:8`) is intentional OpenAI-compat wiring (registry reads `process.env.OPENAI_BASE_URL` with same-literal fallback `registry.ts:59`); centralized + server-only (no client `process.env.ZHIPU/OPENAI` reads — import-graph verified). `SESSION_SECRET` dev fallback is loud (`session/route.ts:53` warn) + prod-fail-closed (`session.ts:60` requires ≥16 chars in prod); legacy unsigned cookies fail closed; 24h expiry; 30/min issuance cap. No `.env*` in repo, placeholders only, secret scan 0 hits. Remaining: no `service_role` guidance by design (never expose); `engines` pin + key-rotation runbook tracked.

## 12. Phase 1 refresh 2026-09-14 (HEAD `bc69d30` + working tree, V2 basis)

> Method: `git rev-parse/branch/log/status` + `npm run lint/typecheck/test/build` fresh + grep audits + 4 parallel read-only subagents (architecture / AI / DB-security-privacy / frontend-UX-tests). No src/ modified in Phase 1.

- **HEAD / toolchain:** `bc69d30eeb1220de4fc78630edf8258728e37f9` on `main` (8 commits ahead of `f37f574`: OAuth bridge, DSA CORS/jsdom pins, middleware→proxy, .env.example). Node `v24.15.0` + npm `11.12.1` local; Docker `node:20-alpine`. Next `16.2.4`, React `19.2.4`, `ai@6.0.168`, `@ai-sdk/* 3.x`, `supabase-js@2.104.1`. Working tree DIRTY (40+ M incl. 12 API routes + interview/dashboard/practice/home/auth pages) — §11's "lint 0" no longer holds for the tree.
- **Baseline re-measured 2026-09-14:** `lint` FAIL — **859 errors / 13301 warnings** (top: `@ts-ignore`→`@ts-expect-error`, unused `handler/__Unused`, `Footer <a>`→`<Link>`, dsa-web `set-state-in-effect` + hooks violations); `typecheck` PASS (0 errors); `test` PASS **25 files / 201 tests**; `build` PASS (Turbopack 4.9s, 43 routes: 18 API + pages, edge-runtime + recharts `-1` warnings only). Verdict: engineering gate is RED on lint only — Phase 2 must zero it without suppressions.
- **Architecture (subagent):** 19 `page.tsx` (only `practice/[id]/page.tsx` Server + `layout.tsx` Server; rest Client-first); 18 API routes (17 POST + `session` POST/GET/DELETE) all behind `guardRequest` (requestId→IP limit→session→maxBytes+Zod, default 25s timeout); `src/ai/prompts/` 19 versioned prompts + `PROMPT_REGISTRY`; providers single-site `registry.ts` (Zhipu glm-4-flash/4.7 + vision, Gemini 2.5-flash/1.5-pro, OpenAI optional); `src/proxy.ts` named `proxy` matcher guards `/dashboard|/setup|/interview|/chat|/recruiter|/practice` + `/api/dashboard`; state `zustand×3` + `AuthContext` (server cookie authority); PWA `@ducanh2912/next-pwa` no hand `sw.js`; observability `logApi` PII-free envelope + `x-request-id` + client `TelemetryProvider`.
- **AI (subagent, 16 routes tabled):** `analyze-interview` is the V2 exemplar (`EvaluationV2Schema` 1-5 + evidence.min(1) + `normalizeEvaluation`); all OTHER scoring lanes still raw 0-100 (`behavior/practice/chunk/star/match/alignment`); `match` vs `alignment` duplicated JD-gap task (double spend, double scale — merge candidate); Chinese hardcode in `match.ts:34`/`context.ts:14`/`analyze-interview route:64`/`interview.ts` Panel; 5/14 builders fenced (`interview/match/alignment/copilot/evaluation`), 9 bare (`behavior/star/practice/code/jd/context/hint/vision/trends/chunk`); `trends/copilot` `generateText`+hand-parse (migrate to `generateObject`); `interview-chat` max attack surface (client `model/starProgress/behavioralTraits`, server-recomputed `loopState` saves it); Culture/Hire generation GONE, display residue 22 hits (report/PrintLayout/SessionDetail/export/eval-compat/trends/setup/interview-HR) — read-only legacy fidelity.
- **DB/Security/Privacy (subagent):** `0001` (UUID+JSONB, 0 RLS/0 index) vs `001` (BIGSERIAL+TEXT[], 11 indexes, 7× `FOR ALL USING(true)` no-TO at `001:203-221`) vs `002` (nullable `user_id`×7 + 7 owner indexes, drops 7 opens, `TO authenticated auth.uid()=user_id` + `TO anon user_id IS NULL` bridge); 0 FKs; `src/` zero `auth.uid()`/zero `.from(` in API routes (all-LLM) + anon singleton direct-DB → IDOR still open on legacy NULL rows + `orama_index resume-index` global key; raw mic/video NOT stored (good), transcript/resume/JD stored Supabase + localStorage + LLM-sent (no server purge — `R-259` open, `PRIVACY 90-day` vs `DATA_POLICY no-purge` drift); rate-limit in-memory single-instance + XFF-trust; body caps set (vision 8MB max face); secrets server-only + prod-fail-closed; `logApi` PII-free.
- **Frontend/UX/Tests (subagent):** journeys mapped (Resume/JD/GreenRoom/Coding/SystemDesign/Completion are in-page, no deep routes); live-score distraction FIXED by default (`LiveStats showAiEstimates=false`, `keyboard.spec` locks it; WPM/Filler still live); Camera optional local-only (no switch/device picker gap); Mic/GreenRoom/LiveCaptions/Calm real, Dyslexia thin (`font-mono` only), Settings mock (`Alex Chen`); tests unit16+integration9+spec18; CI `ci.yml` runs lint→typecheck→test→build→eval-smoke→supabase-free→chromium-smoke-9→audit (mock-journey/keyboard/a11y-visual/privacy/e2e + FF/Safari/Edge matrix NOT in CI); i18n dict ~43 keys (<20% coverage, 100+ hardcoded ZH); axe serious/critical 0 on 3 pages only.
- **H1-H11 delta vs §11:** H1 stale-README stands; H2 lint regressed 0→859 (dirty tree); H3 fake-vision stays fixed (0 metric `Math.random`); H4 generation-fixed/display-residue stands; H5/H6 drift + USING(true)→002-contained stands (live tests still blocked: no docker/staging); H7 CI-exists but smoke-only stands; H8 10-file `generateObject` residue non-blocking stands; H9 ids current stands; H10 bundle/PWA risk stands; H11 secrets posture stands.
