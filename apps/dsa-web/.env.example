# DSA web — frontend env (copy to .env.local to override)
# Backend base URL. Default matches lib/api.ts fallback (http://localhost:8000).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Canonical site URL for robots/sitemap metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3001
